import { ToastContainerComponent } from './toast-container';

describe('ToastContainerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(ToastContainerComponent).toBeTruthy();
  });
});
