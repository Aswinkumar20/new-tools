import { ComingSoonToolComponent } from './coming-soon-tool';

describe('ComingSoonToolComponent', () => {
  it('exposes the component class for routing', () => {
    expect(ComingSoonToolComponent).toBeTruthy();
  });
});
