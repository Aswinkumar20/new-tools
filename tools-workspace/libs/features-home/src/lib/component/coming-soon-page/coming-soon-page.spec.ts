import { ComingSoonPageComponent } from './coming-soon-page';

describe('ComingSoonPageComponent', () => {
  it('exposes the component class for routing', () => {
    expect(ComingSoonPageComponent).toBeTruthy();
  });
});
