import { ModelArchitectureViewerComponent } from './model-architecture-viewer';

describe('ModelArchitectureViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(ModelArchitectureViewerComponent).toBeTruthy();
  });
});
