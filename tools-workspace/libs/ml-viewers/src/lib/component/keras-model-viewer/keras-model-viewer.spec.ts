import { KerasModelViewerComponent } from './keras-model-viewer';

describe('KerasModelViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(KerasModelViewerComponent).toBeTruthy();
  });
});
