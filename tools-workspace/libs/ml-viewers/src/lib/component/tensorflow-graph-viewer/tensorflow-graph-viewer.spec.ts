import { TensorflowGraphViewerComponent } from './tensorflow-graph-viewer';

describe('TensorflowGraphViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(TensorflowGraphViewerComponent).toBeTruthy();
  });
});
