import { TensorVisualizationViewerComponent } from './tensor-visualization-viewer';

describe('TensorVisualizationViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(TensorVisualizationViewerComponent).toBeTruthy();
  });
});
