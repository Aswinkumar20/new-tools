import { PickleViewerComponent } from './pickle-viewer';

describe('PickleViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(PickleViewerComponent).toBeTruthy();
  });
});
