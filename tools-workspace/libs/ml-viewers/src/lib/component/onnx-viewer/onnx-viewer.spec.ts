import { OnnxViewerComponent } from './onnx-viewer';

describe('OnnxViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(OnnxViewerComponent).toBeTruthy();
  });
});
