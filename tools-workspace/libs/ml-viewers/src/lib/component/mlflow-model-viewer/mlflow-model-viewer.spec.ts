import { MlflowModelViewerComponent } from './mlflow-model-viewer';

describe('MlflowModelViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(MlflowModelViewerComponent).toBeTruthy();
  });
});
