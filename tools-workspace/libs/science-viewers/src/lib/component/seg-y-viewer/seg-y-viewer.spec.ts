import { SegYViewerComponent } from './seg-y-viewer';

describe('SegYViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(SegYViewerComponent).toBeTruthy();
  });
});
