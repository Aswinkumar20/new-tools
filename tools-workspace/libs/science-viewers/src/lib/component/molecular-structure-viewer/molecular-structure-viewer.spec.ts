import { MolecularStructureViewerComponent } from './molecular-structure-viewer';

describe('MolecularStructureViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(MolecularStructureViewerComponent).toBeTruthy();
  });
});
