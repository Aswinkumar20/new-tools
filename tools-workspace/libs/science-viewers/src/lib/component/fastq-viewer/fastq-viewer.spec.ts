import { FastqViewerComponent } from './fastq-viewer';

describe('FastqViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(FastqViewerComponent).toBeTruthy();
  });
});
