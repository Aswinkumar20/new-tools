import { RootFileViewerComponent } from './root-file-viewer';

describe('RootFileViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(RootFileViewerComponent).toBeTruthy();
  });
});
