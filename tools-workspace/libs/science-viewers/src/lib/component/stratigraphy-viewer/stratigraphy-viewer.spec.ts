import { StratigraphyViewerComponent } from './stratigraphy-viewer';

describe('StratigraphyViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(StratigraphyViewerComponent).toBeTruthy();
  });
});
