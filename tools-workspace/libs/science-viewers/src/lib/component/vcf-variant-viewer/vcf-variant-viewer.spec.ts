import { VcfVariantViewerComponent } from './vcf-variant-viewer';

describe('VcfVariantViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(VcfVariantViewerComponent).toBeTruthy();
  });
});
