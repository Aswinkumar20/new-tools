import { NetcdfViewerComponent } from './netcdf-viewer';

describe('NetcdfViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(NetcdfViewerComponent).toBeTruthy();
  });
});
