import { SimulationResultViewerComponent } from './simulation-result-viewer';

describe('SimulationResultViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(SimulationResultViewerComponent).toBeTruthy();
  });
});
