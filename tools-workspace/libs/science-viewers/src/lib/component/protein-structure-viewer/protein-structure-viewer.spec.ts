import { ProteinStructureViewerComponent } from './protein-structure-viewer';

describe('ProteinStructureViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(ProteinStructureViewerComponent).toBeTruthy();
  });
});
