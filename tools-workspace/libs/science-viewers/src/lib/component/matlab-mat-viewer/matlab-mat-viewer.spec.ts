import { MatlabMatViewerComponent } from './matlab-mat-viewer';

describe('MatlabMatViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(MatlabMatViewerComponent).toBeTruthy();
  });
});
