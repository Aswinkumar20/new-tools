import { PsdViewerComponent } from './psd-viewer';

describe('PsdViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(PsdViewerComponent).toBeTruthy();
  });
});
