import { RawImageViewerComponent } from './raw-image-viewer';

describe('RawImageViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(RawImageViewerComponent).toBeTruthy();
  });
});
