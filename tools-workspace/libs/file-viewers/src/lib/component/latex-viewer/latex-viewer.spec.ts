import { LatexViewerComponent } from './latex-viewer';

describe('LatexViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(LatexViewerComponent).toBeTruthy();
  });
});
