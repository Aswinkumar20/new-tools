import { AiFileViewerComponent } from './ai-file-viewer';

describe('AiFileViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(AiFileViewerComponent).toBeTruthy();
  });
});
