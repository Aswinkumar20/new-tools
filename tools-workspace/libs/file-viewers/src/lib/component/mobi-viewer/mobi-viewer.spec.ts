import { MobiViewerComponent } from './mobi-viewer';

describe('MobiViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(MobiViewerComponent).toBeTruthy();
  });
});
