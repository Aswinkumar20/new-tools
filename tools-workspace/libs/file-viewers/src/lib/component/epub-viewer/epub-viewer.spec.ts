import { EpubViewerComponent } from './epub-viewer';

describe('EpubViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(EpubViewerComponent).toBeTruthy();
  });
});
