import { RtfViewerComponent } from './rtf-viewer';

describe('RtfViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(RtfViewerComponent).toBeTruthy();
  });
});
