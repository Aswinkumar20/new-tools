import { HeicViewerComponent } from './heic-viewer';

describe('HeicViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(HeicViewerComponent).toBeTruthy();
  });
});
