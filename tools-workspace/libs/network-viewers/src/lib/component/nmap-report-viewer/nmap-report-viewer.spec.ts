import { NmapReportViewerComponent } from './nmap-report-viewer';

describe('NmapReportViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(NmapReportViewerComponent).toBeTruthy();
  });
});
