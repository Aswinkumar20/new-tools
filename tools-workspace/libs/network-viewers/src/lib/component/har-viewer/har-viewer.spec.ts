import { HarViewerComponent } from './har-viewer';

describe('HarViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(HarViewerComponent).toBeTruthy();
  });
});
