import { NetworkTrafficViewerComponent } from './network-traffic-viewer';

describe('NetworkTrafficViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(NetworkTrafficViewerComponent).toBeTruthy();
  });
});
