import { HttpTraceViewerComponent } from './http-trace-viewer';

describe('HttpTraceViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(HttpTraceViewerComponent).toBeTruthy();
  });
});
