import { PcapngViewerComponent } from './pcapng-viewer';

describe('PcapngViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(PcapngViewerComponent).toBeTruthy();
  });
});
