import { DnsLogViewerComponent } from './dns-log-viewer';

describe('DnsLogViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(DnsLogViewerComponent).toBeTruthy();
  });
});
