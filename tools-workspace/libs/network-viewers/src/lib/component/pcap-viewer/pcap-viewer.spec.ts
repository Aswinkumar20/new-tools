import { PcapViewerComponent } from './pcap-viewer';

describe('PcapViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(PcapViewerComponent).toBeTruthy();
  });
});
