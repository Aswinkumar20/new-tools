import { PacketAnalyzerComponent } from './packet-analyzer';

describe('PacketAnalyzerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(PacketAnalyzerComponent).toBeTruthy();
  });
});
