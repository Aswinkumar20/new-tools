import { ProtocolAnalyzerComponent } from './protocol-analyzer';

describe('ProtocolAnalyzerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(ProtocolAnalyzerComponent).toBeTruthy();
  });
});
