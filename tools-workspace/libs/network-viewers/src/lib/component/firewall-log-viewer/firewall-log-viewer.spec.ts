import { FirewallLogViewerComponent } from './firewall-log-viewer';

describe('FirewallLogViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(FirewallLogViewerComponent).toBeTruthy();
  });
});
