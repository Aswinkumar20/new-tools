import { NessusReportViewerComponent } from './nessus-report-viewer';

describe('NessusReportViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(NessusReportViewerComponent).toBeTruthy();
  });
});
