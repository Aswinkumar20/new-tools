import { SarifReportViewerComponent } from './sarif-report-viewer';

describe('SarifReportViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(SarifReportViewerComponent).toBeTruthy();
  });
});
