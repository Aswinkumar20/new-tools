import { ArrowViewerComponent } from './arrow-viewer';

describe('ArrowViewerComponent', () => {
  it('exposes the component class for routing', () => {
    expect(ArrowViewerComponent).toBeTruthy();
  });
});
